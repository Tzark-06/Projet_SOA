/*if somebody enter the room and the light is lower than ??? i want 
the switch to be activated. And if nobody is in the room and the light is on, 
i want the switch to be turned off. If the temperature is higher 
than 25°C and somebody is in the room, I want windows to open.*/



        if(presence.getValue() == 1 && lightData.getValue() < 400 ){ //valeur a changer
            switchLight.setState("ON"); // nom a changer
        }else if(presence.getValue() == 0 && switchLight.getState() == "ON"){
            switchLight.setState("ON");
        }
        if(tempData.getValue()>25 && presence.getValue() == 1){
            windows.setState(1);
        }else if(windows.getState() == 1){
            windows.setState(0);
        }

